# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
.open-next

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
# Ignore local overrides (not committed)
.env.local
.env.*.local
*.env
.env.*

# Committed env files for different environments
# .env - committed (base configuration)
# .env.development - committed (development defaults)
# .env.staging - committed (staging defaults)
# .env.production - committed (production defaults)
# .env.*.example - committed (templates for local overrides)

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma
